<h1>Welcome to our movies list !!!</h1>
<h2><%= @list.name %></h2>

<h3><% @list.bookmarks.each do |bookmark|  %> </h3>
    <%= bookmark.comment %>
    <%= bookmark.movie.title %>
<% end %>

<%= link_to "Add a Bookmark to a movie",  new_list_bookmark_path(@list), class: 'btn btn-primary' %>
